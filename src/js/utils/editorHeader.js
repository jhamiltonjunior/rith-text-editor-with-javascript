const bold = document.createElement('strong')
const italic = document.createElement('')

// textArea.addEventListener('selectionchange', () => {
//   text.textContent = textArea.selectionStart
//   text2.textContent = textArea.selectionEnd
//   text3.textContent = textArea.selected
// })

// if (textArea.selectionStart !== undefined) {
//   const startPosition = textArea.onselectstart
// }
// setBold.addEventListener('click', () => {
//   textArea.innerHTML += '<strong></strong>'
// })
